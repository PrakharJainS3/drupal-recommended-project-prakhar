version: 1.3.0

events:
  build:
    steps:
      - setup-env:
          type: script
          script:
            - touch .git/testfile1.txt
            - mkdir /tmp/acli-push-artifact/
            - mv .git /tmp/acli-push-artifact/
            
