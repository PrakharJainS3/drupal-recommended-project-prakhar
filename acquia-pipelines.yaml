version: 1.0.0
variables:
  global:
    COMPOSER_BIN: $SOURCE_DIR/vendor/bin

events:
  build:
    steps:
      - composer_setup:
          type: script
          script:
            - echo $COMPOSER_BIN
