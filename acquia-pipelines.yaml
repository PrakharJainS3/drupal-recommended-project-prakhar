version: 1.0.0
services:
  - mysql

events:
  build:
    steps:
      - setup:
          type: script
          script:
            - mysql --version
            - sleep 600
            - mysql -u root -proot -e "SHOW VARIABLES LIKE '%vers%'"
